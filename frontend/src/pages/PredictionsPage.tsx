import PagePlaceholder from "../components/PagePlaceholder";

export default function PredictionsPage() {
  return (
    <PagePlaceholder
      title="Predictions"
      phase="Phase 7 (baseline models) + Phase 9 (consensus) surfaced via Phase 14"
    />
  );
}
