# Phase 1: minimal set to run the API skeleton + tests.
# Later phases append their own needs here (scikit-learn/xgboost in Phase 7,
# pandas-ta in Phase 4, shap in Phase 11, transformers+torch for FinBERT in Phase 10, etc.)
# rather than front-loading heavy ML/NLP dependencies before there's code that uses them.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pyjwt==2.10.1
# bcrypt called directly (Phase 13), not via passlib -- passlib's CryptContext probes
# bcrypt.__about__.__version__, which bcrypt>=4.1 removed, breaking hashing entirely. See
# app/auth/security.py's module docstring; the same fix forex-ai-dashboard (docs/architecture.md's
# "lessons carried over") needed for the identical reason.
bcrypt==5.0.0
email-validator==2.2.0
pytest==8.3.4
httpx==0.28.1

# Phase 2: historical data ingestion
# pandas/numpy pins were originally 2.2.3/2.1.3 -- bumped to satisfy pandas-ta's actual
# declared minimum (pandas>=2.3.2, numpy>=2.2.6), which pip's resolver silently enforced
# by upgrading these all the way to pandas 3.0.5 once Phase 10 installed transformers/torch.
# Pandas 3.0.5 itself turned out to conflict with torch's DLL loading on Windows (see the
# pyarrow note below) -- pinned back to the last pandas 2.x line instead.
pandas==2.3.3
numpy==2.2.6
yfinance==1.6.0

# Phase 3: data cleaning, storage, and EDA
# pyarrow 18.1.0 has a real Windows DLL conflict with torch (WinError 1114 loading
# torch/lib/c10.dll) -- reproducible with nothing more than `import pyarrow; import torch`,
# independent of pytest. Fixed by upgrading to 25.0.1; if this resurfaces after a future
# dependency bump, re-check this pairing before assuming it's something else.
pyarrow==25.0.1
matplotlib==3.9.3
jupyter==1.1.1
ipykernel==7.3.0
nbconvert==7.17.1

# Phase 4: technical indicators
pandas-ta==0.4.71b0

# Phase 7: baseline ML models
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.5.3

# Phase 10: FinBERT news sentiment
# torch is CPU-only and NOT on the default PyPI index -- install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# (installing it via a plain `pip install -r requirements.txt` will fail to resolve torch;
# install torch separately first, then the rest of this file).
torch==2.13.0
transformers==5.15.1

# Phase 11: SHAP explainability
shap==0.52.0
