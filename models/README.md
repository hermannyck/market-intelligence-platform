# models/

Trained model artifacts (one per asset × timeframe × model, from Phase 7 onward), plus the
walk-forward run that produced each one. Reproducible from `data/features/` + the training
scripts — not hand-edited.
