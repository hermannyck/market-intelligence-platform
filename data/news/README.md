# data/news/

Historical financial news + FinBERT sentiment scores, timestamp-aware and asset-tagged
(Phase 10). If a live historical news feed isn't available, this holds a clearly documented
sample/historical dataset instead — never data that would leak future articles into a past
prediction timestamp.
