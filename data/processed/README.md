# data/processed/

Cleaned OHLCV data (gaps handled, duplicates removed, timestamps normalized), derived from
`data/raw/` by a reproducible pipeline (Phase 3+). Safe to regenerate/delete — never a source
of truth on its own.
