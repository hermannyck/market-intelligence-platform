# data/features/

Engineered feature sets (indicators, multi-timeframe features, regime, sentiment) built
from `data/processed/` (Phase 5+). Every column is computed using only information available
at that row's timestamp — see `docs/leakage_prevention.md`.
